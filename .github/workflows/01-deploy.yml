name: ClaimSync Deployment

on:
  workflow_dispatch:

concurrency:
  group: claimsync-deployment
  cancel-in-progress: false

jobs:
  deployment:
    name: Verify Deployment Workflow
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: |
          echo "Deployment workflow started successfully."